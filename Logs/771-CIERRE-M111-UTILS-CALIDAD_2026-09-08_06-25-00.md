# Log 771: Cierre M111 Código-De-Calidad — implementación de 35 utilidades

**Fecha:** 2026-09-08
**Hora:** 06:25
**Modelo:** Hy3
**Plataforma:** Kilo Code

## Resumen
Cierre de M111 (Código de Calidad). En el Log 770 se detectó que el módulo figuraba ✅ sobre el plan-inicial (248 ítems) pero su plan-actual tenía **35 ítems [ ] REALES** (utilidades de calidad inexistentes en el código). En esta sesión se implementaron las 35 utilidades y se cerró el módulo (209/209 [x], 0 [?]).

## Cambios Realizados (scripts en `game/isla-ancestral/scripts/utils/`)

### Utilidades
- `math_utils.gd` — `MathUtils`: `distance_squared`, `lerp_value`, `clamp_value`, `normalize_angle` (static).
- `validation_utils.gd` — `ValidationUtils`: `is_valid_position`, `is_valid_item_id`, `is_valid_npc_id`, `is_valid_mission_id`.
- `format_utils.gd` — `FormatUtils`: `format_time`, `format_money`.
- `game_constants.gd` — `GameConstants`: `MAX_INVENTORY_SIZE`, `DAY_DURATION_SECONDS`, `CHUNK_SIZE`, `MAX_PLAYERS`, `MAX_SAVE_SLOTS`, `AUTO_SAVE_INTERVAL_SECONDS`.
- `game_enums.gd` — `GameEnums`: `enum State`, `enum Category`, `enum Priority`.

### Componentes (composición sobre herencia)
- `components/health_component.gd` — `HealthComponent` (Node, señales health_changed/died).
- `components/inventory_component.gd` — `InventoryComponent` (Node, add/remove/count).
- `components/state_component.gd` — `StateComponent` (Node, estado finito simple).

### Patrones
- `state_machine.gd` — `StateMachine` (FSM genérico con callbacks enter/exit).
- `factory.gd` — `Factory` (registro de builders por id).
- `command.gd` — `Command` (acción con guarda opcional).
- `strategy.gd` — `Strategy` (base de estrategia intercambiable).

### Structs de datos (`data/`)
- `player_data.gd` — `PlayerDataStruct`, `item_data.gd` — `ItemDataStruct`, `npc_data.gd` — `NPCDataStruct`, `mission_data.gd` — `MissionDataStruct` (RefCounted; sufijo `Struct` para no colisionar con `class_name ItemData` ya existente).

### Documentación
- `DOCUMENTACION/111-Codigo-De-Calidad/plan-actual/CONVENCIONES-CODIGO.md`: grupos de nodos, layers física/render, composición sobre herencia, máximo 3 niveles de herencia.

## Verificación
- `05-Checklist.md` (plan-actual): 35 ítems pasaron de `[ ]` a `[x]`; total **209/209 [x], 0 [?]** (script de cierre + firma Hy3).
- Búsqueda en el código confirmó los 16 `class_name` y las funciones clave (Grep positivo).
- `EventBus_` (Observer) ya existía en `core/` → ítem cubierto por referencia.
- Tests unitarios/integración: delegados a M112 (suite GdUnit4 existente, 187 cases).

## Nota de limitación
Godot no está disponible en esta sesión (sin binario `godot`), por lo que no se pudo ejecutar el check de compilación ni los tests headless de forma independiente. El código sigue las convenciones GDScript del proyecto (class_name global, static funcs, signals) y evita colisiones de nombre verificadas previamente.

## Archivos Modificados/Creados
- 16 scripts nuevos en `scripts/utils/` (+ subdirs components/, data/).
- `DOCUMENTACION/111-Codigo-De-Calidad/plan-actual/CONVENCIONES-CODIGO.md` (nuevo).
- `DOCUMENTACION/111-Codigo-De-Calidad/plan-actual/05-Checklist.md` (35 ítems → [x], totales corregidos a 209/209, firma).
- `CHECKLIST-GLOBAL.md` — fila M111: Estado → ✅ Completado, progreso 209/209, nota de cierre.
- `Mensajes entre modelos/ESTADO-PARALELO.md` — fila M111 actualizada a ✅.
- `Logs/reservas/771-Hy3-M111.txt` — consumido tras este log.
