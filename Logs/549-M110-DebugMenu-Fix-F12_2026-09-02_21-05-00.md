# Log 549: M110 Debug Menu — Fix del atajo F12 (no estaba cableado) + re-check visual

**Fecha:** 2026-09-02
**Hora:** 21:05
**Modelo:** deepseek-v4-flash-vision-exp
**Plataforma:** Kilo Code

## Resumen

Al re-chequear el debug menu (la verificación visual pedida), se descubrió que **el atajo F12 nunca estuvo implementado** en `debug_menu.gd` (el script no tenía `_unhandled_input`; la apertura de la mañana vino de otra vía). Se implementó el manejo F12 (alternar visible) y se protegió el viewport.

## Cambios Realizados

- `scripts/debug/debug_menu.gd`: `_unhandled_input` con `KEY_F12` (pressed, sin echo) → `alternar()`, con protección `if vp:` de `set_input_as_handled`.
- `scripts/debug/test_debug_menu_headless.gd`: check F12 añadido (simula InputEventKey F12 → `_menu.visible == true`).

## Verificación

- 2 checks OK (instancia + only-debug); el archivo del test se cuelga después en el export de diagnóstico (misma sección problemática del entorno headless de la tarde — el diag export ya fue verificado OK en el Log 403).
- La verificación visual del menú abriendo por F12 queda para la próxima corrida interactiva (win real).

## Archivos Modificados/Creados

- Modificados: `scripts/debug/debug_menu.gd`, `scripts/debug/test_debug_menu_headless.gd`, `DOCUMENTACION/110-Debug-Menu/plan-actual/05-Checklist.md`, `Logs/ULTIMO_NUMERO.txt` (→549)
