# Log 312: M21 Diálogos — Iteración 8 (clima + validación de claves)

**Fecha:** 2026-08-31
**Hora:** 23:52
**Modelo:** Hy3
**Plataforma:** WorkBuddy

## Resumen

Iteración 8 de M21 (Diálogos), ejecutada por Hy3 / WorkBuddy tras bloquear el módulo (CHECKLIST-GLOBAL + ESTADO-PARALELO). Foco: cerrar `[?]` de mi dominio (validación/condiciones de mundo/clima M32).

## Cambios Realizados

1. **Hallazgo de QA (validación):** la condición de clima ya estaba cableada en `world_state_service.gd` (`_get_clima()` → `Weather.get_nombre_clima()`, M32), pero el ciclo de VALIDACIÓN estaba desactivado en silencio: `validate_all_dialogues.gd` tenía `CLAVES_MUNDO = []` y el gate `[VAL-DGV]` de `dialogue_manager.gd` no pasaba allowlist. Un typo como `"climaX"` en un diálogo nunca se detectaba (ni CI ni runtime).
2. **Fuente única de verdad** en `dialog_graph_validator.gd`: constante `CLAVES_MUNDO_BASE` (incluye `clima`, `amistad_`, `flag_`) + helper `_clave_conocida()` que reconoce prefijos; `_validar_cond` ahora SIEMPRE chequea claves desconocidas.
3. **`validate_all_dialogues.gd`:** `CLAVES_MUNDO` resuelto en `_ejecutar()` desde `DialogGraphValidator.CLAVES_MUNDO_BASE` (DRY).
4. **`dialogue_manager.gd` gate `[VAL-DGV]`:** ahora pasa la allowlist ⇒ rechaza claves desconocidas en runtime (cierra H.16).
5. **Nuevo `test_clima_dialogo_m21.gd`:** valida que el validador acepta `clima`, rechaza `climaX`, y que `WorldStateService.get_value("clima")` resuelve contra M32 con fallback "" si M32 ausente.

## Archivos Modificados/Creados

- `game/isla-ancestral/scripts/dialogos/dialog_graph_validator.gd` (allowlist canonica + helper)
- `game/isla-ancestral/scripts/dialogos/dialogue_manager.gd` (gate [VAL-DGV] con allowlist)
- `game/isla-ancestral/scripts/dialogos/validate_all_dialogues.gd` (CLAVES_MUNDO desde validador)
- `game/isla-ancestral/scripts/dialogos/test_clima_dialogo_m21.gd` (nuevo test)
- `DOCUMENTACION/21-Dialogos/plan-actual/04-Codigo.md` (nota iter 8)
- `DOCUMENTACION/21-Dialogos/plan-actual/05-Checklist.md` (3 [?] → [x]; 71/139 → 74/139 + 9 [?])
- `CHECKLIST-GLOBAL.md` (fila M21: progreso 74/139, agente Hy3/WorkBuddy)
- `Mensajes entre modelos/ESTADO-PARALELO.md` (registro iter 8)

## Limitaciones

No se pudo ejecutar el runtime headless (Godot no instalado en el entorno WorkBuddy). La verificación es estática: todas las APIs invocadas existen (`DialogueGraph`, `DialogueNode`, `WorldState.get_value`, `Weather.get_nombre_clima`, `DialogGraphValidator.CLAVES_MUNDO_BASE`). El cambio en el gate solo AGREGA detección de claves desconocidas; los JSON existentes usan solo claves válidas, por lo que no rompe regresión.

## Veredicto

Iter 8 de M21 cierra 3 `[?]` de condiciones de clima/validación (mi especialidad). Módulo queda 🟡 con 9 `[?]` restantes que son dueño de M22/M23/M53/M87 (fuera de mi alcance directo). M21 se desbloquea para el siguiente agente; Hy3/WorkBuddy disponible para otro módulo especialista.
