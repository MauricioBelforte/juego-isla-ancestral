# Log 921: M115 Hardware — iter. agnes (reconciliación + 2 falsos-verdes corregidos)

**Fecha:** 2026-09-15
**Hora:** 20:50
**Modelo:** agnes-3-flash (Sapiens AI)
**Plataforma:** Kilo Code

## Resumen

Segunda iteración del bucle de **agnes-3-flash**. **M115 Hardware** (V0 + data-driven + auditoría
código↔checklist = mi perfil). La auditoría del 2026-09-14 había revertido el `05-Checklist.md` a todos
`[ ]`. Verifiqué contra el **código real** y **corregí 2 tests de falso verde** sin tocar el autoload que
funciona.

## Cambios Realizados

- **Detección de falsos verdes (la hallaza principal):** `test_hardware.gd` (7 `SCRIPT ERROR`) y
  `test_hardware_iter2.gd` (5) llamaban una **API de detección unificada** (`profile`, `get_active_preset`,
  `set_preset`, `apply_deadzone`, `_detector`, `preset_changed`) que el autoload de catálogo
  `hardware_manager.gd` **no expone** → los errores se tragaban y salían con código 0 (falso verde).
- **Fix (bajo riesgo, no toco el autoload):** re-apunté ambos tests a la API que **sí existe**:
  `HardwareProfile` standalone (vía `preload`) + el manager de catálogo (autoload `hardware`), con
  **guardián anti-falso-verde** (`_fin()` por bloque) y las piezas de detección marcadas **DEFERRED a M90**.
- **Verificación headless (godot 4.7.2):** `test_hardware.gd` **21/0** · `test_hardware_iter2.gd` **13/0** ·
  `test_hardware_m115.gd` **17/0** = **51 checks, 0 fallos, 0 `SCRIPT ERROR`**.
- **Docs:** `04-Codigo.md` §Iteración agnes (divergencia + falsos-verdes + autoload duplicado), `05-Checklist.md`
  (puntero de verificación + "Notas del Agente"), `CHECKLIST-GLOBAL` fila 115.

## Hallazgos reales

1. **Divergencia diseño↔implementación:** 04-Codigo original (Nemotron) describe 4 clases con `class_name`
   + `.tres`; lo implementado es un **catálogo JSON** (`hardware_profiles.json`, 3 perfiles) **sin
   `class_name`** (pitfall headless §9.41). `hardware_profile`/`hardware_detector` existen pero **no están
   cableados al autoload** (la wiring es de M90).
2. **2 falsos-verdes corregidos** (51/0 tras retarget, 0 `SCRIPT ERROR`).
3. **Autoload duplicado:** `project.godot` registra `hardware` **y** `HardwareManager` (mismo script) → el
   manager corre 2× al boot. **Documentado, NO corregido** (afecta el boot global; dueño M90/infra).

## Archivos Modificados/Creados

- `game/isla-ancestral/scripts/hardware/test_hardware.gd` (retarget — reescripto, no toca el autoload)
- `game/isla-ancestral/scripts/hardware/test_hardware_iter2.gd` (retarget)
- `DOCUMENTACION/115-Hardware/plan-actual/04-Codigo.md` (§Iteración agnes)
- `DOCUMENTACION/115-Hardware/plan-actual/05-Checklist.md` (puntero verificación + Notas del Agente)
- `CHECKLIST-GLOBAL.md` fila 115 (mod)
- `Mensajes entre modelos/ESTADO-PARALELO.md` (entrada, se agrega)
- `DOCUMENTACION/TAREAS-POR-MODELO/agnes-3-flash/115-Hardware/checklist.md` (mod)

## Estado de M115
🟡 **Liberado (iter. agnes).** Evidencia: 51 checks / 0 fallos / 0 `SCRIPT ERROR`. Secciones B/C/E/G
respaldadas `[x]`; D (M90), F (M57), H (M72/M95), I (M97) + wiring de detección (M90) = `[?]` con dueño.
Flip caja-a-caja de los 132 ítems y corrección del autoload duplicado **delegados** (fuera de mi alcance
sin riesgo). QA cruzado §21.8 pendiente (verificador ≠ autor).
