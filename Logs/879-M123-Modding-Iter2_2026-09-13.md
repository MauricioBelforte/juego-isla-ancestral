# Log 879: M123 Modding — iteración 2 (sobre-cierre corregido + sandbox y esquema M108) — DeepSeek-V4.1-Flash

**Fecha:** 2026-09-13
**Hora:** 19:10
**Modelo:** DeepSeek-V4.1-Flash
**Plataforma:** WorkBuddy
**Módulo:** M123 Modding
**Iteración:** 2
**Estado del módulo:** 🟡 Liberado (101/108 `[x]`; 7 pendientes = producto / UI M89 / Steam M97 / meta)
**Modalidad:** §21.4.7 — dueño anterior (`deepseek-v4-flash-vision-exp`, Kilo Code) inactivo desde 2026-09-02

## Resumen

El módulo estaba marcado como *"Verificado (Log 532): 16/16 tests OK. Módulo operativo"* con **20/106** en la fila global, pero su propia checklist tenía **84 `[x]` y 24 `[ ]` sobre 108 ítems** — y su bloque "Totales" declaraba **"106 ítems, 106 resueltos, 0 pendientes"**. Es decir: un **sobre-cierre** más el clásico desfase entre registros. Además, `test_modding_m123.gd` **empezaba con BOM UTF-8** (violación §28) y `04-Codigo.md` describe la implementación en **C# (Unity)** — el diseño muerto que nunca correspondió al código real en GDScript.

La iteración corrige el sobre-cierre, implementa las reglas que faltaban y añade **sandbox de rutas** y **esquema de assets alineado con M108**.

## Hallazgos

| Afirmación previa | Realidad medida |
|---|---|
| Fila global: 20/106 | checklist real: **84/108** |
| "Totales": 106 ítems, 0 pendientes | **24 `[ ]` reales** |
| Cabecera: "(110 ítems)" | **108** |
| Convención: "`[ ]` = completado. `[ ]` = pendiente" | dos veces `[ ]` → ambigua |
| `04-Codigo.md`: archivos `.cs` en `Assets/_Project/` | diseño **muerto**; el código real es `scripts/modding/*.gd` |
| test con BOM | **BOM UTF-8** (§28) |

## Qué se construyó

| Archivo | Cambio |
|---|---|
| `scripts/modding/mod_sandbox.gd` | **NUEVO** — `ruta_segura()` (anti path traversal), `nombre_asset_valido()` / `extension_compatible()` (**espejo** de M108), `validar_assets()` (esquema + 10 MB + duplicados) |
| `scripts/modding/mod_validator.gd` | **Reescrito** — **códigos `E01..E14`**, `validar_paquete()` (corrupto/readme/schema), duplicados con override |
| `scripts/modding/modding_manager.gd` | **Extendido** — `resolver_prioridad()` (menor prioridad se omite + motivo) y `es_compatible_update()` (M118) |
| `scripts/modding/test_modding_m123.gd` | **Reescrito** — **11 bloques A-K, 69 checks**, marcadores `_fin()`, **BOM eliminado** |
| `data/mods/mod_manifest.json` | **Extendido** — `schema_version`, `author`, `prioridad`, `readme`, `assets[]` por id, límites |
| `DOCUMENTACION/123-Modding/plan-actual/06-Plan-Testings.md` | **NUEVO** — CP-01..CP-20 + casos no cubiertos |
| `DOCUMENTACION/123-Modding/plan-actual/07-Resultados-Testings.md` | **NUEVO** — suites, cobertura, bugs |
| `DOCUMENTACION/123-Modding/plan-actual/08-Limites-Politicas-Y-Herramientas.md` | **NUEVO** — límites, políticas, códigos, uso de herramientas, telemetría sin PII |

## Verificación

```
=== Resumen M123: 69 checks, 0 fallos ===
TEST M123 OK — todos los checks pasaron
```

**69 checks, 0 fallos, 3/3 corridas, 0 `SCRIPT ERROR`.** El test cubre: manifiesto, compatibilidad por build, conflictos, validación de catálogo, errores de manifiesto, **sandbox (8 checks)**, **esquema M108 (13)**, **paquete corrupto (5)**, **prioridad (4)**, **updates M118 (4)** y **códigos (4)**, más 11 marcadores de bloque.

## Bugs reales cazados por el test adversarial

El test construye fixtures que **deben** fallar. Al hacerlo encontró 2 bugs en el código nuevo de esta misma iteración:

1. **Extensión derivada del campo equivocado.** `extension_compatible(nombre)` tomaba la extensión del `nombre`, pero el esquema M108 define el `nombre` **sin** extensión (la extensión está en la `ruta`) → todo asset daba `E09`. Corregido: `extension_compatible(nombre, ruta)`.
2. **`JSON.parse_string` devuelve números como `float`.** El chequeo de `prioridad` exigía `TYPE_INT` y rechazaba `10` (que llega como `10.0`) → `E14` espurio. Corregido: se acepta `TYPE_INT` o `TYPE_FLOAT`.

Ambos habrían pasado desapercibidos con un test tautológico.

## Defectos del módulo corregidos

1. BOM UTF-8 en `test_modding_m123.gd` (§28) → eliminado.
2. Bloque "Totales" falso (106/0 pendientes) → 108 / 101 / 7 reales.
3. Convención ambigua (`[ ]` dos veces) → `[x]` completado / `[ ]` pendiente / `[?]` no resuelto.
4. Cabecera "(110 ítems)" → 108.

## Hallazgo cruzado (no es de M123)

**M108 declara sus pruebas headless como bloqueadas** (`[?]` en su checklist: *"Godot finaliza sin generar salida"*). **Eso es falso en este entorno**: en esta sesión se ejecutaron headless con salida correcta los tests de M116, M117, M101 y M123 (el mismo `--headless --path … --script …`). M108 tiene un bug en su runner o usó una invocación equivocada. **No lo toqué** (no es mi módulo): queda registrado para quien lo reclame.

## Lo que NO se pudo verificar (honestidad)

- Desempaquetado real de un `.zip`/`.mod` (no implementado).
- Instalación en `user://mods/` (no implementado).
- Integración con Steam Workshop M97 (V2) y UI de mods M89.

Los 7 `[ ]` restantes son **decisiones de producto** (criterios del GATE), **UI M89**, **Steam M97** y **meta** — no tooling/datos.

## Registros

- Fila 123 de `CHECKLIST-GLOBAL.md` → 🟡 Liberado · 101/108 · DeepSeek-V4.1-Flash.
- Fila en `Mensajes entre modelos/ESTADO-PARALELO.md`.
- Checklist personal sincronizada en sitio (17 cambios; 84/0/24 → 101/0/7).
- Reserva `Logs/reservas/879-DeepSeek-V4.1-Flash-M123.txt` (liberada al cierre).

**Firmado:** DeepSeek-V4.1-Flash / WorkBuddy — 2026-09-13
