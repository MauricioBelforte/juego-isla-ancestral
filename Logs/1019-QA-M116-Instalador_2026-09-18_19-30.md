# Log 1019: QA M116-Instalador (verificacion §21.8 — QA pendiente del Log 1014)

**Fecha:** 2026-09-18
**Hora:** 19:30
**Modelo:** Atria-Dawn-Preview
**Plataforma:** Kilo Code

## Resumen

QA §21.8 de M116-Instalador, pedido **explicitamente** por DeepSeek-V4.1-Flash al cerrar la iter. 3
(Log 1014: "QA §21.8 de la iter. 3 pendiente (verificador ≠ autor)"). **Veredicto: ✅ MANTIENE —
0 flips, 0 correcciones necesarias.** Es el modulo mas limpio que audite en el ciclo: todos los
claims se verifican de punta a punta con binario real, y el autor fue honesto hasta en lo que no
tenia (icon.ico ausente, reportado en vez de inventado).

## Cambios Realizados

### Verificacion ejecutada (no presuncion)

1. **`test_installer_m116.gd` (scripts/installer/) re-ejecutado con binario real:**
   **18 checks, 0 fallos** (el claim era 15 — hay MAS de los documentados, no menos).
   Cubre: InstaladorConfig autoload, 3 plataformas, 5 pasos, requisitos disco/RAM, gating de
   plataformas no soportadas, pasos duplicados/inexistentes, instalacion incompleta y completa.

2. **`scripts/build/test_instalador_m116.gd` (la ruta EXACTA del gate CI) re-ejecutado:**
   **15 checks, 0 fallos** + invoca al ValidadorInstalador sobre el repo real:
   **"el validador ejecuto al menos 40 checks (ejecuto 61)"** + 7 asserts de deteccion de errores
   (V1 seccion, V5 contenido, V11 BOM, V8 uninstall, V3 version, V6 preset).
   **El claim "15/0 x3, validador 61 checks" es VERDADERO.**

3. **Gate CI (`quality.yml:275`) verificado como DURO de verdad:**
   `godot --headless --script scripts/build/test_instalador_m116.gd 2>&1` **sin `|| true`** —
   el fix de la trampa 75 (Log 1014) esta aplicado. Verifique ademas que la ruta del gate
   (`scripts/build/`) es donde realmente esta el test (existe una copia paralela en
   `scripts/installer/` con 18 checks — ambas pasan).

4. **Validaciones estaticas sobre artefactos reales:**
   - **V11 (sin BOM):** `setup_windows.ps1` 11551 B, `uninstall_windows.ps1` 6360 B,
     `code_signing.bat` 2438 B, `verificar_requisitos.ps1` 4609 B — todos BOM=False y muy por
     encima del umbral anti-regresion de 800 bytes (la iter. 1 los dejo en 3 bytes de puro BOM).
   - **V3 (sync version):** `#define AppVersion "0.0.6"` en `IslaAncestral.iss:19` coincide con
     `config/version="0.0.6"` de `project.godot`. El fix del Log 946 (M117 iter. 3) esta aplicado:
     `tools/ci/bump_version.py:190` tiene `_set_version_in_installer_iss()` que actualiza
     `#define AppVersion` en `installer/*.iss` en cada bump (5 referencias al .iss).
   - **V7 (rutas coherentes):** preset `../build/windows/isla-ancestral.exe` ≡ BuildDir del .iss
     `..\game\build\windows` ≡ referencia del PowerShell. Coherentes.
   - **V8 (no borra partidas):** `[UninstallDelete]` con comentario explicito de NO borrar
     savegames; los datos viven en `%APPDATA%\Godot\app_userdata\isla-ancestral`, fuera de `{app}`.
   - **V10 (firma):** `code_signing.bat` usa `signtool` (8 menciones); los parametros PS
     documentados (BuildDir/InstallDir/NoShortcuts/DryRun + Force/PurgeUserData) todos presentes.

5. **Checklist 192/192** sin un solo `[?]` ni `[ ]`; totales **honestos y ya corregidos** por el
   autor (linea 268: "Resueltos: 192 [x] · Con dueño externo: 0 · Pendientes: 0").

### Lo que NO existe (honesto, reportado, no flip)

- **`installer/icon.ico` ausente** — DeepSeek lo reporto como dueño M46 en vez de inventar un
  .ico falso. Confirmo la ausencia: es la unica brecha, esta documentada en el diseño
  (03-Diseno §S.1/§S.6-8) y **no es un sobre-cierre**. Sigue siendo decision correcta: un .ico
  inventado por un agente seria peor que la ausencia declarada.

### Comparacion con mi tipologia (leccion 21 aplicada)

M116 es el caso opuesto a M11/M12 (sobre-cierre sistemico) y distinto de M38 (bug de dato en
nucleo real): aqui **no hay nada que corregir**. El autor aplico el patron que yo mismo
recomiendo en mis QAs: totales reales, gate duro verificado, artefactos con bytes reales
verificados, y reporte honesto de la unica brecha. **Mi veredicto es pura confirmacion.**

## Archivos Modificados/Creados

- `CHECKLIST-GLOBAL.md` — fila 116 liberada (agente → —) con veredicto de QA.
- `Logs/1019-QA-M116-Instalador_2026-09-18_19-30.md` — este log.
- `DOCUMENTACION/TAREAS-POR-MODELO/atria-dawn/BACKLOG-MASTER.md` — iter 17 registrada (log
  pendiente de actualizar al cerrar).

## Cuenta acumulada

**167 flips + 190 restauraciones de metadata en 11 modulos** (M116 suma 0 flips — modulo limpio).
QA de ✅ con QA pendiente explicito: **M116 resuelto**. Pendientes similares: M52 (iter. 6,
"§21.8 pendiente" — VFX, fuera de mi encaje C).

## Iter 18 — siguiente en la cola

M126 Marketing-Legal (🟡 liberado por agnes Log 981, 4/101) todavia no esta ✅. Opciones:
re-QA de 🟡 con QA previo del mismo modelo (M124 Contenido-Generado, M127 Copyright, M68
Transporte — todos con "✅ Liberado iter. N" de un solo autor), o buscar modulos 🟢
data-driven implementables por mi (patron M164).
