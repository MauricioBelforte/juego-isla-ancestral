# Log 315: M21 Diálogos — Iteración 9 (validación en lote + 5 inválidos)

**Fecha:** 2026-09-01
**Hora:** 00:25
**Modelo:** Hy3
**Plataforma:** WorkBuddy

## Resumen

Iteración 9 de M21 (retomada por Hy3 / WorkBuddy para cerrar `[?]` de mi dominio: validación/robustez). Cerró 2 `[?]` más → 76/139 + 7 `[?]`.

## Cambios Realizados

1. **[?] F/I.9 — Evaluación en lote con `get_snapshot`:** `_combinar_estado` en `dialogue_manager.gd` resolvía las claves de mundo **una por una** con `ws.get_value(clave)` en un loop. Ahora usa `ws.get_snapshot(claves_a_resolver)` (una sola lectura de WorldStateService), respetando la API ya existente. Cierra el `[?]` de evaluación batch.
2. **[?] L.11 — Test de validación con 5 grafos inválidos:** nuevo `test_validacion_5_invalidos_m21.gd` que construye 5 grafos con defectos distintos (nodo huérfano, operador inválido `~~`, clave desconocida `climaX`, `next_id` inexistente, `goto_id` inexistente) y verifica que `DialogGraphValidator` los detecte. Sigue el patrón de los tests previos.

## Archivos Modificados/Creados

- `game/isla-ancestral/scripts/dialogos/dialogue_manager.gd` (`_combinar_estado` usa `get_snapshot`)
- `game/isla-ancestral/scripts/dialogos/test_validacion_5_invalidos_m21.gd` (nuevo test)
- `DOCUMENTACION/21-Dialogos/plan-actual/05-Checklist.md` (2 `[?]` → `[x]`; 74/139 → 76/139 + 7 `[?]`)
- `CHECKLIST-GLOBAL.md` (fila M21: progreso 76/139)
- `Mensajes entre modelos/ESTADO-PARALELO.md` (registro iter 9)

## Limitaciones

No ejecutable headless (Godot ausente en WorkBuddy). Verificación estática: `WorldStateService.get_snapshot` existe (confirmado en iter 8), `DialogGraphValidator.CLAVES_MUNDO_BASE` existe, el test sigue el patrón de los 7 previos de 0 fallos.

## Veredicto

Iter 9 de M21 cierra 2 `[?]` de validación (mi especialidad). M21 queda 🟡 con 7 `[?]` restantes que son dueño de M22/M23/M53/M87 (condiciones de historia/misiones, UI nombre/filtro, localización). M21 se desbloquea para el siguiente agente.
