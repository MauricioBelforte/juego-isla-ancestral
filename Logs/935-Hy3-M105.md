# Log 935 — M105 Telemetría de Gameplay — §21.8 (QA cruzado, verificador ≠ autor)

**Modelo (verificador):** hy3 (Tencent Hunyuan) / WorkBuddy
**Modelo (autor):** DeepSeek-V4.1-Flash / WorkBuddy (Log 926, iter. 7)
**Fecha:** 2026-09-16
**Módulo:** 105-Telemetria-De-Gameplay
**Reserva:** `Logs/reservas/935-Hy3-M105.txt`
**Estado de salida:** 🔵 VERIFICADO (§21.8) — 120 `[x]` / 45 `[?]` / 0 `[ ]`

---

## 1. Re-grounding (anti sobre-cerrado §24)

Confirmado por glob que existen los 5 artefactos citados en
`game/isla-ancestral/scripts/telemetry/`:

- `telemetry_director.gd` ✓
- `test_telemetry.gd` ✓
- `test_telemetria_iter5.gd` ✓
- `test_telemetria_iter6.gd` ✓
- `test_telemetria_iter7.gd` ✓

`stub_analytics_director.gd` está presente pero es **huérfano** (0 referencias; los 4
suites usan su propia inner class `_AnalyticsStub`). Fuera de alcance (§21.4; dueño
ox-alpha).

No sobre-cerrado: cada `[x]` del checklist tiene un archivo de código real detrás.

## 2. Verificación headless (re-ejecutada por hy3, no heredada)

Binario: Godot 4.7.2 (`Godot_v4.7.2-stable_win64_console.exe`, dentro de
`Godot_v4.7.2-stable_win64.exe/`). Cada suite corrida **×3**.

| Suite | Checks | ×3 | EXIT | SCRIPT ERROR en `scripts/telemetry/` |
|---|---|---|---|---|
| `test_telemetry.gd` | 16/0 | ✔ | 0 | 0 |
| `test_telemetria_iter5.gd` | 10/0 | ✔ | 0 | 0 |
| `test_telemetria_iter6.gd` | 11/0 | ✔ | 0 | 0 |
| `test_telemetria_iter7.gd` | 27/0 | ✔ | 0 | 0 |

Todas las cifras coinciden **EXACTAMENTE** con las afirmadas en Log 926 §2 / §4.4.
**0 `SCRIPT ERROR`** atribuibles a `scripts/telemetry/` en ninguna de las 12 corridas.

## 3. Guardián anti-falso-verde (verificado en código fuente)

- `test_telemetry.gd`: `const CHECKS_MINIMOS := 16` (línea 24); si `_checks < 16` →
  `[FAIL] ... aborto silencioso dentro de un bloque`.
- `test_telemetria_iter5.gd`: `const CHECKS_MINIMOS := 10` (línea 20).
- `test_telemetria_iter6.gd`: `const CHECKS_MINIMOS := 11` (línea 27).
- `test_telemetria_iter7.gd`: bloques con `_fin()` (autoload, limpieza, constantes,
  metrica_house/puzzle/seal/viaje, complete_puzzle_ruta, session_duration, optout,
  senales, sin_pii) + `_resumen()` encolado vía `call_deferred` (trampa 61: un aborto
  de `_ejecutar` no deja el proceso colgado hasta el timeout). Si un bloque no llama
  `_fin()`, el resumen nombra los faltantes y fuerza EXIT 1.

Esto cubre la **trampa 63** (un `SCRIPT ERROR` dentro de un helper NO detiene
`_ejecutar`): el piso `CHECKS_MINIMOS` detecta checks "comidos" por un aborto parcial.
El autor ya lo documentó (Log 926 §4) y lo confirmo estructuralmente.

## 4. CI (`quality.yml`)

`grep` de `scripts/telemetry` en `.github/workflows/quality.yml` → los 4 suites
cableados en el job `test-suite` (líneas 251-254):

```
godot --headless --script scripts/telemetry/test_telemetry.gd 2>&1
godot --headless --script scripts/telemetry/test_telemetria_iter5.gd 2>&1
godot --headless --script scripts/telemetry/test_telemetria_iter6.gd 2>&1
godot --headless --script scripts/telemetry/test_telemetria_iter7.gd 2>&1
```

El job corre con `bash -e`, así que un `quit(1)` de cualquier suite falla el job. ✔
Afirmación del autor (Log 926 §5) confirmada.

Discrepancia menor de documentación (no bloquea): el comentario del job en `quality.yml`
dice "iter7 = 22 checks" (línea 242) pero el suite ejecuta **27**. El número
autoritativo es el que imprime el test (27). Anotado para el dueño del CI.

## 5. Honestidad del re-marcado (Log 926 §3.4 y §6)

- **Citas falsas `03-Diseno.md` §3.4/§3.5:** `grep -n "^#" 03-Diseno.md` → el doc solo
  tiene §1-§6 (Arquitectura, Estado interno, API pública, Flujo, Privacidad, Testing).
  **NO existe** ninguna §3.4 ni §3.5. La reparación de las 4 citas por el autor es
  veraz. (Mismo patrón de causa raíz que M127 — notas que citan secciones inexistentes.)
- **Conteo 120/45/0:** `scripts/verificar_checklist.py` → independientemente
  `📋 105-Telemetria-De-Gameplay: [x] 120 / [ ] 0 / [?] 45`. Coincide con el
  re-marcado del autor. El criterio declarado (`[x]` = cubierto por ≥1 aserción de un
  suite corrido ×3 con 0 fallos; `[?]` = dueño nombrado, no implementable en V0) es
  sólido y auditable.

## 6. Delta de entorno (hallazgo del autor vs. estado actual)

Log 926 §8.1 reportó 8 `SCRIPT ERROR` en TODO run headless por un Parse Error activo en
`res://scripts/debug/debug_menu.gd` (autoload `DebugMenu`, dueño atria-dawn / M110). En
MI verificación headless **0 `SCRIPT ERROR` en absoluto**. Causa: `debug_menu.gd` fue
corregido entretanto — las declaraciones sin tipo `var npc :=` / `var res :=` citadas
(líneas 483/514 del Log) ahora son `var npc: Node` / `var res: Variant` (líneas 500/531
actuales), y las `PackedStringArray(...).join()` desaparecieron. El hallazgo de Log 926
era real en su momento; hoy el autoload ya no falla al parsear. Esto **NO** afecta el
veredicto de M105: el código de M105 es limpio (0 SCRIPT ERROR en `scripts/telemetry/`)
y los 8 errores nunca apuntaban a M105.

## 7. Veredicto §21.8

✅ **VERIFICADO.** Verificador (hy3/WorkBuddy) ≠ autor (DeepSeek-V4.1-Flash).
Re-grounding OK, 4 suites headless re-corridas ×3 EXIT 0 con las cifras exactas del
autor (16/0, 10/0, 11/0, 27/0) y 0 SCRIPT ERROR propios; guardián anti-falso-verde
presente (CHECKS_MINIMOS 16/10/11 + `_fin()`/`call_deferred` en iter7); CI cablea los
4 suites; conteo 120/45/0 confirmado por `verificar_checklist.py`; citas falsas
reparadas verazmente. Cumple §21.8.

Caveats:
1. Los 45 `[?]` son por diseño no implementable en V0 (dueño nombrado) — no es deuda de M105.
2. 0 llamadores externos de los `track_*` a hoy (verificado por el autor con grep) — M105
   expone la API testeada; el llamador es de cada módulo consumidor. Afirmación del autor,
   no revertida.
3. `debug_menu.gd` ya no produce los 8 SCRIPT ERROR que el autor midió (corregido entretanto);
   el estado de M110 no es responsabilidad de M105.
4. Discrepancia menor en comentario de `quality.yml` ("iter7 = 22" vs 27 reales).

---

**Firmado:** Hy3 / WorkBuddy (Tencent Hunyuan) — Log 935, §21.8
**Reserva:** `Logs/reservas/935-Hy3-M105.txt`
