# Log 360: M35 nota de diseño pendiente + M40 Infraestructura (GameFlow + SceneManager + integridad)

**Fecha:** 2026-08-31
**Modelo:** Deepseek V4 Flash
**Plataforma:** Kilo

## Resumen
1. M35 (Minería) quedó **pausado con nota de diseño pendiente** (decisión del usuario: capa
   superficial indestructible vs solo nodos de mineral — antes de implementar destrucción voxel).
2. M40 (Infraestructura) implementado: GameFlowManager (máquina de estados del flujo),
   SceneManager (cambio de escenas con anti doble-click), autorregistro de dominios RF11 en el
   Bootstrap y verificación de integridad DOM-INF. Test headless 0 fallos.

## Cambios Realizados

### M35 — Nota de diseño (pausado)
- `DOCUMENTACION/35-Mineria/plan-actual/05-Checklist.md`: nota "DECISIÓN DE DISEÑO PENDIENTE"
  con las dos opciones (A capa indestructible / B solo nodos) y recomendación del agente.
- `CHECKLIST-GLOBAL.md` fila M35 → 🟡 Con dudas (pausado con nota, Log 298).

### M40 — Infraestructura
- `scripts/core/game_flow_manager.gd` — **NUEVO autoload "GameFlowManager"**: máquina de estados
  BOOT/MENU/CARGANDO/MUNDO/PAUSA/ERROR con transiciones validadas (§1.1); señal estado_cambiado;
  `en_juego()`. Ruta del prototipo: BOOT→MUNDO directo (sin menú aún; M89/M63 completarán el
  flujo — documentado en el código).
- `scripts/core/scene_manager.gd` — **NUEVO autoload "SceneManager"**: cambiar_escena(ruta)
  con validación de existencia, bloqueo anti doble-click (§8 AGENTS), change_scene_to_file
  DEFERIDO (pitfall §9.20/§9.25), señales de inicio/completado/fallo.
- `scripts/core/bootstrap.gd` — Modificado (M40 §1.1/§3.1):
  - `DOMINIOS_ESPERADOS`: catálogo de 9 dominios de juego (RF11).
  - `_autoregistrar_dominios()`: autorregistro por contrato (deferred, no toca scripts de otros).
  - `verificar_integridad_dominios()`: DOM-INF-FALTANTE advertencia (has(), sin spamear errores).
  - `_verificar_game_flow()`: BOOT→MUNDO al arrancar (transición BOOT→MUNDO agregada al GFM).
- `project.godot` — autoloads GameFlowManager + SceneManager registrados.
- `scripts/core/test_infraestructura.gd` — **NUEVO** test: máquina de estados (transiciones
  válidas/inválidas), SceneManager (rechazos), integridad de 9 dominios. 0 fallos.

### Verificación runtime (MCP Godot)
- `[M40] Flujo: 0 -> 3` + `GameFlowManager -> MUNDO`.
- 11 servicios autorregistrados (9 dominios + game_flow + scene_manager).
- `[Bootstrap] DOM-INF integridad OK: 9 dominios de juego verificados`.
- Captura: mundo cargado sin regresión visual (FPS 60).

## Archivos Modificados/Creados
| Archivo | Acción |
|---------|--------|
| `scripts/core/game_flow_manager.gd` | Creado |
| `scripts/core/scene_manager.gd` | Creado |
| `scripts/core/bootstrap.gd` | Modificado (autorregistro + integridad + flujo) |
| `scripts/core/test_infraestructura.gd` | Creado |
| `project.godot` | Modificado (2 autoloads) |
| `DOCUMENTACION/35-Mineria/plan-actual/05-Checklist.md` | Nota de diseño pendiente |
| `CHECKLIST-GLOBAL.md` | M35 pausado + M40 |
| `Logs/ULTIMO_NUMERO.txt` | Modificado (297 → 298) |
| `Logs/298-M40-Infraestructura-M35-Nota_2026-08-31_04-20-00.md` | Creado (este log) |

## Validación
- `test_infraestructura.gd`: 0 fallos (estados, scene manager, integridad).
- Regresión: M53 (0), M87 (0) — limpio. M16 (del otro agente) tiene 1 fallo NO relacionado:
  su test `_test_season_changed_runtime` emite señal `estacion_cambio` pero su
  `_refrescar_estacion_actual` relee la estación REAL del calendario (flaky por mundo real).
  NO se tocó (trabajo de otro agente — observación para coordinación).
- Rutas de captura: `capturas/cap_40_infra_arranque.png`.

## Pendientes del M40
- Reordenar autoloads al orden canónico del diseño (Bootstrap primero) — requiere auditar
  dependencias de _ready de todos los autoloads; riesgo alto, diferido.
- Progreso visual de carga M63 (SceneManager ya emite señales para él).
- Pantalla de menú real M89 (GameFlow ya tiene el estado MENU disponible).
