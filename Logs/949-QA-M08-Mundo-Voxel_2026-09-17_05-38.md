# Log 949: QA cruzado M08 Mundo Voxel — MANTIENE ✅ (veredicto diferenciado) + fixes de documentacion

**Fecha:** 2026-09-17
**Hora:** 05:38
**Modelo:** Atria-Dawn-Preview
**Plataforma:** Kilo Code

## Resumen

Cuarto QA del ciclo mundo-voxel (despues de M09 Log 944 y M10 Log 945). Veredicto
**diferenciado**: M08 **mantiene ✅** con 0 flips. Su checklist es honesto en su alcance
de diseño y tiene codigo vivo (block_type.gd). Pero su documentacion mintia sobre
archivos y firmas — corregida in-situ.

## Por que M08 mantiene el ✅ (a diferencia de M09/M10)

- Todos los items del checklist usan verbos de **diseño** ("Diseñar/Documentar/Definir")
  y la nota de cierre del 05-Checklist explicita: "la validación física (greedy por
  tipo, radio de carga, medición de remesh) es responsabilidad del hito M1 y de M61;
  el diseño queda cerrado aqui". No hay claims de integracion cableada falsos.
- **Hay codigo vivo**: `block_type.gd` (30 constantes AIR=0 ... MUD=29 +
  SHALLOW_WATER=30, categorias, tool_required, drops, debug colors) es central para
  island_generator (M10), la library de main_island.gd y M15 recursos.
- 03-Diseno.md (116 l.) es sustantivo y cubre las 8 secciones del checklist.

## Cambios Realizados (fixes de documentacion, no flips)

- `04-Codigo.md` §2: marcado in-situ que de los 5 archivos listados **4 no existen**
  (voxel_world.gd, block_validation.gd, world_events.gd, diff_store.gd) y que la
  fachada VoxelWorld + registro en ServiceRegistry **nunca se materializaron**. La
  edicion de bloques la implementan `scripts/tools/tool_controller.gd` +
  interaction_manager.gd.
- `04-Codigo.md` §3: firmas de contrato corregidas a las reales
  (`tool_controller.try_extract() -> Dictionary`,
  `try_place(block_id, metadata) -> bool`; no hay objeto `world` ni
  `set_block_puzzle`); confirmado que `world.block_placed` SI esta en EventBus
  (boot: "[M92] Triggers EventBus conectados: [..., world.block_placed, ...]").
- `04-Codigo.md`: seccion "6. QA Cruzado" anexa (historial intacto).
- `CHECKLIST-GLOBAL.md` fila 08: claim "librería 21 bloques" corregido a
  **31 modelos** (ids 0-30, main_island.gd:96-139); sello de QA anexado.

## Hallazgos

1. **4 de 5 archivos previstos no existen** (patron de "paths fantasma" como M09).
2. **Firmas de contrato diseñadas vs reales divergen**: un consumidor que siga el doc
   escribirla codigo roto. Las reales estan en tool_controller.gd (Hy3).
3. **Claims stale de MiMo**: "BLOCK_AIR a BLOCK_LAVA" — LAVA no existe;
   "BlockCatalog.new().build_voxel_library()" — el metodo es `_build_library()`
   (privado) y BlockCatalog es codigo muerto (Log 945).
4. **Lo bueno**: paleta Maldivas aprobada por el usuario aplicada en main_island.gd
   (SAND #F5F0E1, GRASS #55711E, SHALLOW_WATER (0.25,0.82,0.78)); VoxelBoxMover del
   jugador live en boot; block_type.gd consistente con la library real (31 modelos);
   boot headless limpio.
5. **Pendiente real detectado**: `has_gravity` (arena/grava sueltas) esta definido en
   BlockType pero **ningun codigo lo consume** — gravedad de bloques sin implementar.

## No verificado en esta iter

- Edicion E/Q en runtime (colocar/extrair con input): requiere jugador activo, no
  replicable headless. Las funciones subyacentes existen y estan tipadas.

## Archivos Modificados/Creados

- `DOCUMENTACION/08-Mundo-Voxel/plan-actual/04-Codigo.md` (fixes §2/§3 + sec. 6 QA)
- `CHECKLIST-GLOBAL.md` (fila 08: fix bloque + sello QA)
- `Mensajes entre modelos/ESTADO-PARALELO.md`
- `DOCUMENTACION/TAREAS-POR-MODELO/atria-dawn/BACKLOG-MASTER.md` (iter 7)
- `Logs/949-QA-M08-Mundo-Voxel_2026-09-17_05-38.md` (este log)

## Recomendaciones para el proximo agente

1. Si se materializa la fachada VoxelWorld, alinearla con las firmas reales (o migrar
   tool_controller a ella) — hoy hay dos contratos divergentes.
2. Decidir el destino de BlockCatalog muerto (borrar o cablear — Log 945).
3. Implementar el consumo de `has_gravity` (gravedad de bloques sueltos) o marcarlo
   como pendiente de M1.
4. Verificar que M19/M22/M38/M43 consuman tambien `block_removed`/`block_modified`
   (solo se vio `world.block_placed` en el boot).
