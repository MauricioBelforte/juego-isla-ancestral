# Log 395: Fix de boot global — M120 DlcManager (print % sin tupla) detectado en la sesión QA + registro guía 07

**Fecha:** 2026-09-01
**Hora:** 23:42
**Modelo:** deepseek-v4-flash-vision-exp
**Plataforma:** Kilo Code

## Resumen

Durante la sesión QA #01 (M101) se detectó que el juego quedaba congelado en el splash por un **Debugger Break de boot** introducido por el nuevo `dlc_manager.gd` (M120, Log 409 de deepseek-v4-flash): `SCRIPT ERROR: not enough arguments for format string in operator '%'` en `_ready`. Se corrigió mecánicamente, se verificó la suite completa y el boot real, y se documentó el error en la guía 07 §9.62 (con renumeración de la 9.60/9.61 conflicto con glm).

## Cambios Realizados

- **Fix:** `game/isla-ancestral/scripts/dlc/dlc_manager.gd:24` — `print("[M120] DlcManager listo (%d DLC, %d bundles)" % config.get("dlcs", []).size(), bundles.size())` → argumentos envueltos en tupla `% [ ... , ... ]` (sin cambio de lógica).
- **Registro de error:** guía 07 §9.62 (formato del `%` + causa + solución + evidencia); la entrada 9.60 duplicada de glm se renumeró a 9.61.
- **ESTADO-PARALELO:** aviso 🟢 RESUELTO del bug de boot (quedó como referencia para el dueño M120).

## Verificación

- `run_tests.gd` → **RESULTADO: ÉXITO** (0 fallos, exit 0) con `[M120] DlcManager listo (2 DLC, 1 bundles)` en el boot.
- Mundo real (run_project + captura): **FPS 60**, spawn OK, jugador + hotbar + calendario + **nuevo widget de fecha/hora visible** — todo cargando. Captura: `tools/mcp/godot-mcp/capturas/101-QA-General/cap_101_2026-09-01_23-40-00_mundo_postfix.png`.

## Notas

- El "splash eterno" de las pruebas anteriores (y del intento de captura F12/menú) era este break, no una limitación de presentación D3D12: la ventana no exhibía frames porque el juego estaba detenido en el Depurador. La limitación de captura (ventana ocluida) sigue documentada en guia-para-agentes como condición secundaria.
- El hallazgo confirma el valor del QA por logs (M101): el break no aparecía en las capturas (splash) y sí en el output del depurador.

## Archivos Modificados/Creados

- Modificados: `scripts/dlc/dlc_manager.gd` (fix), `DOCUMENTACION/07-GUIA-GODOT.md` (§9.62, renumeración 9.61), `Mensajes entre modelos/ESTADO-PARALELO.md` (aviso resuelto), `Logs/ULTIMO_NUMERO.txt` (→395)
- Creados: captura postfix (no versionada)

## Cierre

- Boot global restaurado (el árbol vuelve a correr con TODOS los módulos activos — M41-M44, M63, M72, M88, M106-M109, M117-M120, M122, M150).
- La sesión QA #01 (Log 394) + este fix (Log 395) + los hallazgos V4 documentados cierran el ciclo de vision/QA de la línea.
