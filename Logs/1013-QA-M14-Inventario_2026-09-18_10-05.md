# Log 1013: QA M14-Inventario (verificacion §21.8 — tercer QA, modelo distinto)

**Fecha:** 2026-09-18
**Hora:** 10:05
**Modelo:** Atria-Dawn-Preview
**Plataforma:** Kilo Code

## Resumen

Tercer QA cruzado §21.8 de M14-Inventario (previos: glm-5.3-flash Log 550 — autor; Hy3 Log 697 +
Log 951 — **mismo modelo**, re-ejecutaron tests pero no auditaron la UI). **Veredicto: ✅ MANTIENE
con correcciones** — 146 items: **142 [x], 4 [?] honestos, 0 [ ]**. El nucleo de datos es genuino
y robusto; los 4 flips son todos de UI/mundo y **no afectan la API** que consumen M13/M16/M17/M39.

## Cambios Realizados

### Verificacion ejecutada (no presuncion)

1. **Tests re-ejecutados con binario Godot 4.7.2 real (headless):**
   - `test_inventario.gd`: **0 fallos** — hotbar 6 slots (asignar/limpiar/seleccionar/ciclar wrap
     +/-, fuera de rango rechazado), esporas (agregar 0/negativo no decrementa), persistencia
     round-trip (slots/slot_activo/esporas), version 0 ignorada.
   - `test_inventario_iter5.gd`: **0 fallos** — cofres, overlay dos paneles, transferencias,
     tutoriales, restore, compatibilidad iter 4.

2. **API de `inventario_service.gd` (420 l.) auditada funcion a funcion** (26 funciones publicas):
   add_item/remove_item/count_item(include_house)/has_free_space/used_slots/total_slots/
   move_item/swap_items/split_stack/sort_container(4 modos)/agregar_items/remover_items/
   consume_for_crafting(recipe)/give_gift/equip_tool/use_toolDurability/discard_item(confirmado)/
   donate_item/is_mission_item/validate_all_containers + ISaveProvider completo
   (get_section_name/get_save_data/restore_save_data) registrado en SaveManager.
   - Verificado: `discard_item` con confirmacion de 2 pasos (P24), `is_mission_item` protege
     descarte (H.10), `consume_for_crafting` con verificacion previa (I.1).

3. **Nucleo de datos verificado:**
   - `inventario_contenedor.gd`: to_dict/from_dict con version, senales changed por slot y por
     contenedor, rechazo de ids desconocidos con log DOM-14.
   - `container_type.gd`: enum BOLSILLO/MOCHILA/CASA/COFRE/ALMACEN/CORREO + tamanos
     (bolsillo 24, mochila +16, casa 60->120, cofres 16/28/40, almacen 240) — coincide con P2.
   - `inventory_slot.gd`: stack_max/cantidad/favorito/bloqueado/instancia/vaciar.
   - `hotbar_state.gd` (117 l.): HOTBAR_SIZE=6, slots -1=vacio, ciclar wrap, persistencia.

4. **UI viva (no codigo muerto):**
   - `inventory_layer.gd` (449 l.): grid dinamico (Button.new()), pestanas de categoria con
     contadores, busqueda sanitizada con memoria, sort con memoria (_sort_mode), tooltip lazy
     0.5s, descarte con confirmacion, indicador usado/total, **pausa suave del game_time** al
     abrir (l. 423-427), liberacion de foco y cierre limpio.
   - `hotbar_widget.gd` (182 l.) presente en `ui/hud.tscn`.
   - `tooltip_service.gd` (157 l.).

### 4 flips a [?] (todos UI/mundo, no del nucleo)

- **E.7** "Acciones contextuales por slot: usar, equipar, vender, donar, descartar" — **SOLO
  DESCARTE esta cableado en la UI** (`_discard_button` l. 120-124). Grep de `usar`/`equipar`/
  `vender`/`donar` en `inventory_layer.gd` = **0**. **Los metodos del servicio SI existen**
  (equip_tool l. 296, use_toolDurability l. 317, donate_item l. 366) — falta el cableado
  UI->servicio. Flip a [?] con dueño: UI (M53/M14-UI).
- **E.11** "Soporte completo de gamepad" — grep `gamepad` en la capa = **0**. Solo teclado
  (acciones `inventario`/`pausa`). El servicio es agnostico al input (sin _input). Flip a [?].
- **H.2** "Recoleccion con bolsillo lleno: pickup flotante en el mundo" — `inventario_service.gd:359`
  emite la senal `item_removed` "para que el mundo cree un pickup", pero grep global de
  `item_removed` muestra que **ningun consumidor crea un nodo pickup** (los unicos conectados:
  save_manager dirty-flag + HUD refresh). Flip a [?].
- **H.3** "Pickups flotantes con cantidad y desvanecimiento recogible" — **0 codigo**: no existe
  ninguna escena/script `pickup` en el proyecto (grep `pickup`/`Pickup` solo encuentra comentarios
  y un nombre de sonido). Dependencia de H.2. Flip a [?].

### 2 correcciones in-situ (no flips)

- **E.1** citaba `slot.tscn` — **no existe**; la grilla se construye con `Button.new()` dinamico
  en `_crear_slots`. [x] mantenido con redaccion corregida (la grilla SI funciona: verificada por
  test_inventario 0 fallos).
- **J.9** "Apertura del inventario <= 5 ms" — plausible (UI lazy, construccion O(slots)) pero
  **sin medicion registrada**. [x] mantenido con la salvedad documentada.

### Nota de proceso (leccion 20 confirmada otra vez)

El aviso de reconstruccion del checklist (l. 9, minimax-m3-free 2026-08-26) admitia explicitamente
que los [x] de UI/almacenamiento/integraciones **"permanecieron [x]"** al regenerar el archivo,
sin verificar implementacion. Los QAs de Hy3 (Log 697 + 951) re-ejecutaron los tests — 0 fallos,
confirmado por mi — pero **no grepiaron la UI**: dieron por buenos los 4 items que yo flippy.
Exactamente el patron "presencia de archivos" de la leccion 20. El conteo de la fila global
(140/140) tambien estaba mal: el checklist tiene **146 items** (nunca se actualizo tras las
iteraciones 4/5).

## Archivos Modificados/Creados

- `DOCUMENTACION/14-Inventario/plan-actual/05-Checklist.md` — 4 flips a [?], 2 correcciones
  in-situ, linea Totales anadida (faltaba), bloque de verificacion QA completo.
- `CHECKLIST-GLOBAL.md` — fila 14 liberada (agente -> —), progreso 140/140 -> 142/146 real.
- `Logs/1013-QA-M14-Inventario_2026-09-18_10-05.md` — este log.

## Cuenta acumulada

**163 flips + 190 restauraciones de metadata en 10 modulos** (M09 7 · M10 16 · M11 73 · M12 53 ·
M38 6 · M13 2 doc · M36 2 doc · M14 4 · M29 metadata · M08 0).

## Iter 16 — siguiente en la cola

Pool restante: **M164 Isla-De-Combate-Endgame** (🟢 0/130, complejidad 4 — solo la parte
data-driven; excluyo VFX/visual por encaje C). Despues: M126 Marketing-Legal (si se marca ✅).
La cadena de QA de ✅ con sello Hy3 ya esta agotada en mi encaje (M32/M29/M13/M36/M14 hechos).
