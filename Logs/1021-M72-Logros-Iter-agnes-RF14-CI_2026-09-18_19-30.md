# Log 1021: M72 Logros — iter. agnes acotada (RF14 data-integrity + gate CI + bug M39)

**Fecha:** 2026-09-18
**Hora:** 19:30
**Modelo:** agnes-3-flash (Sapiens AI)
**Plataforma:** Kilo Code

## Resumen

Iteración del bucle V0/data-driven de **agnes-3-flash** sobre **M72 Sistema de Logros** (relevo del
`🟡 Con dudas 86/185`, sin dueño). **Alcance acotado y aditivo:** cerrar el item RF14 abierto con un
**test aditivo**, cablear los tests de M72 al gate CI y **flag del bug M39** (BUG-050). **NO toco el
core de M72** (`achievement_service.gd` pertenece a glm-5.3).

## Cambios Realizados

- **Nuevo `scripts/logros/test_logros_m72_statids.gd` (aditivo):** cierra el item RF14 abierto
  "validar que las estadísticas referenciadas existan en el perfil de M71" como **equivalente
  headless**: cada `stat_id` de `logros.json` debe resolver contra (a) el vocabulario M71
  (`data/progresion/hitos.json`, recursivo `stat_min`+`compuesta.hijos`) **o** (b) un prefijo dinámico
  documentado (`amistad_max_*` M20 / `pescar_*` M34, registrados en runtime por `achievement_service`).
  → **9 checks, 0 fallos, exit 0, 0 `SCRIPT ERROR`**. Evidencia actual: 5 stat_ids resuelven vía
  M71-known, 1 (`amistad_max_catalina_oso`) vía dinámico.
- **`quality.yml`:** `test_logros.gd` + `test_logros_m72_statids.gd` **no estaban** cableados →
  añadidos al **gate duro** (test-suite). El core de logros queda protegido por CI.
- **`05-Checklist.md` M72:** item RF14 marcado `[x]` con evidencia + §"Reserva actual" (Log 1021) +
  §"Iteración agnes".
- **`04-Codigo.md` M72:** §"Iteración agnes".
- **`11-BUGS.md`:** **BUG-050** registrado `[?] Delegado` a M39/glm.
- **`CHECKLIST-GLOBAL.md` fila 72:** `🟡 Con dudas` → **`🟡 Liberado (iter. agnes RF14+CI)`** 87/185.

## Verificación (godot 4.7.2 headless)

- `godot --headless --path game/isla-ancestral --script res://scripts/logros/test_logros_m72_statids.gd`
  → **9 checks, 0 fallos, exit 0, 0 `SCRIPT ERROR`**.
- `godot --headless --path game/isla-ancestral --script res://scripts/logros/test_logros.gd` →
  **0 fallos, exit 0** (1 `SCRIPT ERROR` **ajeno** de M39 al boot — ver BUG-050).

## Hallazgo delegado (BUG-050, M39/glm — no lo corrijo)

- `catalogo_tiendas.gd:63` → `SCRIPT ERROR: Nonexistent function 'size' in base 'Callable'` (llama
  `.size()` a un Callable) → contamina **todo** boot de autoloads.
- `WARNING: [M39] 'tienda_general': item_id inexistente en M15: piedra_caliza` → el catálogo M39
  referencia un item M15 que no existe.
- **Delegado** a M39 (en curso por glm) en `11-BUGS.md` `[?] Delegado`. No lo corrijo para no pisar
  su trabajo.

## Lo que NO hice (dueños)
- Los 8 items con dueño **M53/M46** (integración visual, notificaciones UI) siguen `[ ]`.
- La herramienta de **editor** para RF14 (si M72 la quiere) = **M72/glm** (yo di el equivalente
  headless).

## Estado de M72
🟡 **Liberado (iter. agnes RF14+CI, acotada).** 87/185. Mi parte (test RF14 aditivo + gate CI + flag
BUG-050) entregada. Los 8 items M53/M46 siguen con dueño. QA cruzado §21.8 del Log 1021 pendiente
(verificador ≠ agnes-3-flash).

## Nota operativa (V3 + error propio)
- Log 1021 tomado del pool V3 (`NUMEROS_DISPONIBLES.txt`, línea 1).
- **Corregí un error propio:** en la edición del `BACKLOG-MASTER` agnes reemplacé en vez de insertar,
  perdiendo las filas de historial de iter 11 (M128) e iter 12 (M66) → **restauradas** en esta sesión.
