# Log 582: Jabalí Adulto + Sistema de Etapas joven/adulto (M36)

**Fecha:** 2026-09-03
**Hora:** 05:50
**Modelo:** GLM 5.3 (z-ai)
**Plataforma:** Kilo Code

## Resumen

Feedback del usuario: "el jabalí es chico; dejemos este como joven y
después lo agrandes y sea adulto — esa idea me gusta". Implementado como
SISTEMA DE ETAPAS en `jabali_npc.gd`: una sola propiedad `etapa`
("joven"/"adulto") configura automáticamente escala, velocidad, rebote y
duración de pausas. La escena ahora tiene los DOS: el joven original
(232, 240, 1.5×) y un adulto nuevo (285, 224, **2.3× ≈ 1.5 m de cruz**,
trote pesado 1.2 m/s, pausas largas 3-8 s).

## Cambios Realizados

- `jabali_npc.gd`: `@export_enum("joven","adulto") var etapa` + diccionario
  `_CFG_ETAPA` (escala/velocidad/rebote/pausas por etapa) aplicado en
  `_aplicar_etapa()` al `_ready`. El log ahora imprime la etapa:
  `[Jabali joven]` / `[Jabali adulto]`.
- `main_island.tscn`: nodo `JabaliNPC` renombrado conceptualmente a joven
  (`etapa = "joven"`) + nodo nuevo `JabaliAdultoNPC` (`etapa = "adulto"`)
  en (285, 224).
- **Fix bloqueante ajeno #1:** `achievement_service.gd:140` —
  `pez.get("id", "")` (Object.get() no acepta default en Godot 4, parser
  error que frenaba el boot). Corregido con get + null-check.
- **Bug ajeno registrado, NO tocado:** `main_island.gd:159` asigna
  `full_load_distance` a `VoxelMesherBlocky` (propiedad inexistente —
  crash al bootear). Introducido por otra sesión en paralelo
  (optimización de terreno). Registrado como **Bug 023 [?] Delegado** en
  `DOCUMENTACION/11-BUGS.md` con fix propuesto. Regla §21.4: no pisar
  módulo en curso de otro agente.

## Verificación (V4)

- Boot llega hasta el crash ajeno con AMBOS jabalíes ya vivos y logueados:
  `[Jabali joven] troteando por la isla (spawn 232, 240) — 4 patas, cola: si, cabeza: si`
  `[Jabali adulto] troteando por la isla (spawn 285, 224) — 4 patas, cola: si, cabeza: si`
- Mi scope (etapas + jabalíes) verificado: nodos resueltos, animaciones OK.
- El crash del arranque es del Bug 023 (ajeno) — ver 11-BUGS.md.

## Archivos Modificados/Creados

- `game/isla-ancestral/scripts/fauna/jabali_npc.gd` (etapas)
- `game/isla-ancestral/scenes/main_island.tscn` (+JabaliAdultoNPC, etapa joven explícita)
- `game/isla-ancestral/scripts/logros/achievement_service.gd` (fix get())
- `DOCUMENTACION/11-BUGS.md` (Bug 023 delegado)

## Pendiente / Próximo agente

- **Bug 023 (main_island.gd:159):** eliminar la línea o aplicar el patrón
  null-check del VoxelTerrain. Bloquea el arranque completo del juego.
- Validación visual del usuario de la escala adulta 2.3× (V1).
- El patrón de etapas puede replicarse en tortuga/cangrejo si el usuario
  quiere crías/adultos de otras especies.
