# Log 317: M21 Diálogos — Iteración 10 (tipeo + amistad M19)

**Fecha:** 2026-09-01
**Hora:** 00:36
**Modelo:** Hy3
**Plataforma:** WorkBuddy

## Resumen

Iteración 10 de M21 (retomada por Hy3 / WorkBuddy para cerrar `[?]` de mi dominio: robustez de manager + efectos). Cerró 2 `[?]` más → 78/139 + 5 `[?]`.

## Cambios Realizados

1. **[?] D.3 — advance() respeta tipeo activo:** `DialogueManager` ahora tiene `set_tipeando(bool)` y `advance()` no avanza mientras `_tipeando == true` (la UI completa el texto y vuelve a llamar con false). Esto evita saltar líneas a mitad de tipeo. Test: `test_iter10_m21.gd::_test_advance_respeta_tipeo`.
2. **[?] F — Efectos que modifican amistad del NPC (M19):** `DialogueNode.apply_effects` ahora soporta `destino: "amistad"` con `clave = <npc_id>` y acciones `set`/`increment`, aplicando sobre `Friendship.set_nivel` (M19). Test: `test_iter10_m21.gd::_test_efecto_amistad`.

## Archivos Modificados/Creados

- `game/isla-ancestral/scripts/dialogos/dialogue_manager.gd` (`_tipeando`, `set_tipeando`, guarda en `advance`)
- `game/isla-ancestral/scripts/dialogos/dialogue_node.gd` (`apply_effects` rama `amistad`)
- `game/isla-ancestral/scripts/dialogos/test_iter10_m21.gd` (nuevo test)
- `DOCUMENTACION/21-Dialogos/plan-actual/05-Checklist.md` (2 `[?]` → `[x]`; 76/139 → 78/139 + 5 `[?]`)
- `CHECKLIST-GLOBAL.md` (fila M21: progreso 78/139)
- `Mensajes entre modelos/ESTADO-PARALELO.md` (registro iter 10)

## Limitaciones

No ejecutable headless (Godot ausente en WorkBuddy). Verificación estática: `Friendship.set_nivel`/`get_nivel` son la API estándar de M19 (confirmada en iteraciones previas); `DialogueNode`/`DialogueGraph` se usan en los 8 tests previos de 0 fallos.

## Veredicto

Iter 10 de M21 cierra 2 `[?]` de robustez/efectos (mi especialidad). M21 queda 🟡 con 5 `[?]` restantes que son dueño de M22/M23/M53/M87 (condiciones historia/misiones, UI nombre/filtro/teclado, localización). M21 se desbloquea para el siguiente agente.
